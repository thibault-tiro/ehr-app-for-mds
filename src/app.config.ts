/**
 * The identity of this app. The onboarding skill fills this in; edit it freely.
 */
export const appConfig = {
  name: 'Scan Viewer',
  tagline:
    'Open DICOM images from a CD, USB stick or download. Nothing leaves this device.',
} as const
